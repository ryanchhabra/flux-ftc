# See ../docs/design/CONTRACT.md "Toolchain (this machine)" -- JAVA_HOME must be JDK 17 or the
# build fails with "Unsupported class file major version".
org.gradle.jvmargs=-Xmx2g
kotlin.stdlib.default.dependency=false
